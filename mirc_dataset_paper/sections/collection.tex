%=======================================================================
% Requires: booktabs, multirow, amsmath, amssymb, siunitx, todonotes
% Bib keys: s3e, cumulti, deepsense6g, inscope, opv2v, v2v4real, dairv2x
%=======================================================================
\section{Dataset Design and Collection}
\label{sec:collection}

\subsection{Environments}
\label{sec:environments}

Data collection was conducted on the Guangfu Campus of National Yang Ming Chiao
Tung University (NYCU), at indoor and outdoor sites. Site selection was governed
by three factors, each associated with a quantity the release reports per frame
rather than with a qualitative descriptor. \textit{Viewpoint}: whether the site
admits vantages displaced from one another---an elevated, persistent
infrastructure position, and mobile nodes able to separate in the ground
plane---characterised by the range of aspect angles $\beta$ realisable between
an ego and a helper, whether that helper is infrastructure or a peer.
\textit{Appearance}: illumination, texture, and clutter density, which determine
the range beyond which $\mathrm{visible}(i)$ ceases to imply
$\mathrm{observed}(i)$ (Eqs.~\eqref{eq:visible}--\eqref{eq:observed}).
\textit{Radio propagation}: whether structure intervenes between a node and the
access point, characterised by first-Fresnel-zone clearance. Prior
sensing-and-communication datasets infer blockage from received power
\cite{deepsense6g}; we compute clearance geometrically and validate it against
measured RSSI (Section~\ref{sec:results}), so that link state is an independent
label rather than a threshold on the signal it explains.

Each site is surveyed and mapped once as a dense point cloud, with all node
poses expressed in a single anchored map frame, so that occlusion,
co-visibility, and link geometry are evaluated against a common geometric
representation. The sequence characterised here was recorded at the MIRC site,
an indoor laboratory comprising one surveyed elevated camera--radar mast, a
\SI{5.75}{\giga\hertz} access point positioned behind furniture, and static
seating that constitutes the occluding structure.

\todo{Site-overview figure: map cloud, surveyed infrastructure pose, AP
position, and both mobile trajectories, one panel per site.}

\subsection{Collection Protocol}

\subsubsection{Roles}
One mobile agent is designated the \textit{ego} and is viewpoint limited by
design, either by trajectory---routed such that structure intervenes between it
and the annotated objects---or by sensing budget, carrying the shorter-range
depth sensor. The second mobile agent is the \textit{partner}. Ego assignment
alternates across runs, so that a reported condition is attributable to the
geometry rather than to a platform; for the single sequence characterised here,
both ego assignments of the same recording are reported, which is the strongest
form of this control obtainable from one run.

Infrastructure nodes are never assigned the ego role, their function being the
elevated, persistent vantage that no mobile node can sustain. Existing
multi-robot and roadside datasets collaborate between sensors at a common
height---$\Delta h = 0$ in S3E~\cite{s3e} and InScope~\cite{inscope}---so the
elevation asymmetry is absent by construction, and assigning the infrastructure
node as ego would reinstate that symmetry.

\subsubsection{Trajectory Paradigms}
\label{sec:paradigms}
A paradigm specifies a trajectory geometry together with the collaboration
condition that geometry is intended to induce. The vocabulary originates in
multi-robot SLAM, where trajectory design is established practice:
S3E~\cite{s3e} names four collaborative trajectory paradigms and grades each
against loop-closure opportunity using filled and open circles, and
CU-Multi~\cite{cumulti} treats inter-trajectory overlap as a controlled design
variable. Collaborative perception datasets instead treat the relative geometry
between collaborating agents as an emergent property of the recording. In
vehicle-to-vehicle settings it follows from how the collecting vehicles happened
to drive, whether simulated \cite{opv2v} or recorded on public roads
\cite{v2v4real}; in vehicle-to-infrastructure settings, from sensor placement
and the ego vehicle's approach \cite{dairv2x}. Per-frame poses are necessarily
released, since feature warping depends on them, but the geometry they encode is
never declared in advance, stratified across splits, or used as a reporting
condition. Even where relative pose is deliberately diversified at collection
time \cite{v2v4real}, it is marginalised at evaluation, and reported accuracy
averages over whatever mixture of configurations a recording contained.

Neither community, then, releases a per-frame measurement of the collaboration
condition obtained: S3E's grading is an assertion about a run, CU-Multi's
overlap is obtained by re-running a single platform four times, so no
instantaneous geometry exists to measure, and perception datasets ship the
ingredients without the condition. We retain the vocabulary of the former and
supply the measurement neither provides. Each paradigm is a predicate over
per-frame quantities computed from the map, the annotations, and the node poses,
and released with the recordings.

\paragraph{Per-observation predicates.}
Consider one annotated object at one frame. Let $e$ denote the ego and $j$ range
over the remaining nodes. For node $i$, let $\omega_i \in [0,1]$ denote the
fraction of rays cast from $i$'s sensor origin to the object surface that are
blocked by mapped structure, $r_i$ the range to the object, and $n_i$ the
evidence returns falling within the annotated box (LiDAR points, depth points,
or projected pixel area, according to the node's primary modality). With
$\tau = 0.5$,
%
\begin{align}
\mathrm{visible}(i) &\equiv \text{in FoV} \;\wedge\; \omega_i < \tau,
\label{eq:visible}\\
\mathrm{observed}(i) &\equiv \mathrm{visible}(i) \;\wedge\; n_i \ge n_{\min,i}
\;\wedge\; r_i \le r_{\max,i}.
\label{eq:observed}
\end{align}
%
Visibility is a geometric condition: an unobstructed line exists. Observation is
an evidence condition: the sensor returned support sufficient for a detection,
within the range at which that modality still returns anything. A node may
remain visible and unobserved throughout a sequence, and
Section~\ref{sec:results} shows this to be the dominant failure mode in the
present data.

Occlusion has been quantified on infrastructure sensors previously:
InScope~\cite{inscope} reports an occlusion degree $\xi_D$ over roadside LiDAR,
computed for a fixed vantage between sensors at common height and summarised per
dataset rather than released per frame, and therefore unavailable for
conditioning an evaluation. Here $\omega_i$ is defined per (frame, node, object)
and released as a column.

A node $j \ne e$ is a \textit{helper} if $\mathrm{visible}(j)$, and the best
helper $j^\star$ is that of lowest $\omega$. The aspect angle $\beta$ is
subtended at the object between the rays to $e$ and to $j^\star$, so $\beta$
near $0^\circ$ indicates a redundant viewpoint and $\beta > 90^\circ$ a
complementary one. Each frame further records the co-visibility fraction $C$,
being objects visible to at least two nodes over those visible to at least one,
and, for each link, the minimum first-Fresnel-zone clearance against the same
occupancy grid, normalised by the Fresnel radius; $\mathrm{clear}(a,b) < 0.6$
is the obstructed-link criterion.

\paragraph{Paradigm assignment.}
Table~\ref{tab:paradigms} lists the paradigms. They are evaluated in order and
the first match is assigned, so the ordering encodes a precedence: a condition
combining occlusion with a degraded link is reported as such rather than
absorbed into the plain occlusion class. Here $v_o$ is the object speed,
$v_{r,i}$ its radial velocity relative to node $i$, and $v_{\min}$ the Doppler
resolution floor of the radar, $v_{\min} = k\lambda / (2 N_c T_c)$ with
$k \approx 1.5$.

\begin{table}[t]
\centering
\caption{Trajectory paradigms. Evaluated in order; first match assigned.
$\tau = 0.5$; helper and $j^\star$ as defined in Section~\ref{sec:paradigms}.
The status column states what the present release supports: \checkmark{}
computed and released, $\circ$ computed but not folded into the classifier,
$\times$ undefined in the absence of moving targets.}
\label{tab:paradigms}
\small
\begin{tabular}{@{}clll@{}}
\toprule
\# & Paradigm & Condition & Status \\
\midrule
1 & Doppler diversity   & $\lVert v_o \rVert > 0.3 \;\wedge\; \lVert v_{r,e}\rVert < v_{\min} \;\wedge\; \max_j \lVert v_{r,j}\rVert \ge v_{\min}$ & $\times$ \\
2 & Occlusion $\times$ link & $\neg\,\mathrm{visible}(e) \;\wedge\; \text{helper} \;\wedge\; \mathrm{clear}(e,j^\star) < 0.6$ & $\circ$ \\
3 & Occlusion           & $\neg\,\mathrm{visible}(e) \;\wedge\; \text{helper}$ & \checkmark \\
4 & Handover            & $\lvert \{ i : \mathrm{visible}(i) \} \rvert = 1$ & \checkmark \\
5 & Link stress         & $\text{helper} \;\wedge\; \mathrm{clear}(e,j^\star) < 0.6$ & $\circ$ \\
6 & Co-observation      & $\mathrm{visible}(e) \;\wedge\; \text{helper}$ & \checkmark \\
7 & Unstructured        & none of the above & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Labels are descriptive, not prescriptive.}
A paradigm label is assigned per frame from measured metrics, not declared once
per run: a sequence intended as co-observation contains occlusion frames
wherever the geometry produced them, and those frames are labelled accordingly.
Consequently, evaluation strata are cut by the raw metrics and never by the
label. Performance is reported against $\omega$, $\beta$, $r$, $C$, and link
clearance directly; the label serves to describe a run in one word and to permit
intent to be audited. Each run additionally records an
\texttt{intended\_paradigm} in its metadata, and the agreement between intended
and measured is the design check reported in Section~\ref{sec:results}. It
quantifies how reliably a trajectory plan produces the condition for which it
was written, which is precisely what a $\bullet/\circ$ grading~\cite{s3e}
asserts without measuring.

\paragraph{Use of the released quantities.}
The per-frame quantities are keyed by (\texttt{t\_ns}, \texttt{node},
\texttt{obj\_id}), so that any evaluation over the recordings can be joined to
the condition under which each observation was made. They admit three uses.
They serve as strata: accuracy is reported within bands of $\omega$, $r$,
$\beta$, $C$, or link clearance rather than averaged over the mixture a
recording happens to contain, and a missed object is separable into one the ego
could not see and one it could see but for which it returned insufficient
evidence. They serve as a join between sensing and communication: link state
and perception need are expressed on a common clock, so the value of a
transmission can be assessed at the frame at which it would have been made.
And they serve as an objective: aspect angle, co-visibility, and clearance are
quantities against which a viewpoint, transmission, or placement policy can be
scored directly, so that candidate infrastructure positions and motion policies
are evaluated against recorded trajectories rather than asserted.

\paragraph{Standing parameters.}
The geometric pipeline is frozen across sequences: $\tau = 0.5$; occupancy voxel
\SI{10}{\centi\metre}; 200 rays per object, node, and frame; statistical outlier
removal at 20 neighbours and $2\sigma$; a voxel is occupied only at $\ge 8$
points and a ray blocked only on two consecutive occupied voxels; occluder hits
within \SI{0.6}{\metre} of a sensor origin and occupancy within \SI{0.7}{\metre}
of either link endpoint are disregarded, both nodes and the access point being
themselves present in the map; evidence floors of 1 LiDAR point, 50 depth
points, and 400\,px$^2$; analysis range caps of \SI{20}{\metre} (Ouster),
\SI{4.5}{\metre} (ZED), \SI{5}{\metre} (RealSense), and \SI{20}{\metre}
(infrastructure pixels), each calibrated from the range at which median evidence
falls to its floor; obstructed link below $0.6\,r_1$; carrier frequency read per
frame from the WiFi status stream (\SI{5.75}{\giga\hertz}). Density
thresholding, consecutive-hit testing, and endpoint clearance are workarounds
for map noise and for fixed nodes appearing as mapped structure; they are stated
because they affect the reported values, and a re-analysis against a
survey-grade map should permit their removal.

\paragraph{Per-sequence descriptor.}
Table~\ref{tab:seq} gives the resulting descriptor: one row per ego assignment,
with the design fields spanning both rows and the measured collaboration
geometry reported separately for each. Every sequence in the release is
described in this form, and the columns are frozen so that sequences remain
comparable as the release grows.

\begin{table*}[t]
\centering
\caption{Per-sequence descriptor. Each sequence contributes one row per ego
assignment: the left block records the design, the right blocks the measured
collaboration geometry. Values shown are those of \texttt{coop2\_0828} and are
interpreted in Section~\ref{sec:results}. $^\ast$ staged ego. Episodes are
contiguous intervals of $\neg\,\mathrm{visible}(e)$ of at least
\SI{0.5}{\second}, with the number covered by a helper in parentheses. $\beta$
is measured to the infrastructure node. Range rescue is reported under RGB-D
evidence. NLoS aggregates obstructed and degraded frames on the ego--AP link.
An em-dash denotes a quantity undefined for the sequence, not a zero.}
\label{tab:seq}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lllllccccccccc@{}}
\toprule
\multicolumn{5}{c}{Design} & \multicolumn{4}{c}{Geometry} &
\multicolumn{3}{c}{Rescue rate} & \multicolumn{2}{c}{Link, scene} \\
\cmidrule(r){1-5}\cmidrule(lr){6-9}\cmidrule(lr){10-12}\cmidrule(l){13-14}
Sequence & Site & Paradigm & Stress & Ego &
$C$ & $\beta$ ($^\circ$) & $\beta > 90^\circ$ & Episodes &
Occl. & Range & Dopp. &
NLoS (\%) & Obj. \\
\midrule
\multirow{2}{*}{\texttt{coop2\_0828}} & \multirow{2}{*}{MIRC} &
\multirow{2}{*}{co-observation} & \multirow{2}{*}{---} &
\texttt{m1}$^\ast$ & 0.5 & 149 & 0.67 & 9 (6) & 0.10 & 0.29 & --- & 33 & 2 \\
 & & & & \texttt{m2} & 0.5 & 29 & 0.33 & 8 (5) & 0.02 & 0.39 & --- & 18 & 2 \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Release Composition}
\label{sec:conditions}

Table~\ref{tab:inventory} inventories the present release. It comprises one
sequence, and its scale is stated directly rather than as a frame count treated
as a sample size: \num{733} synchronised frames over \SI{156}{\second} contain
\num{9} and \num{8} occlusion episodes under the two ego assignments, and the
episode is the unit at which occlusion claims are supported.

\begin{table}[t]
\centering
\caption{Composition of the present release. Deliberate difficulty is induced by
the protocol; incidental difficulty arose from the geometry without staging, and
is reported separately because unstaged occurrences constitute the stronger
evidence.}
\label{tab:inventory}
\small
\begin{tabular}{@{}ll@{}}
\toprule
Sequences / sites & 1 / 1 (MIRC, indoor) \\
Duration, frames & \SI{156}{\second}, \num{733} frames at \SI{5}{\hertz} \\
Nodes & 2 mobile + 1 surveyed elevated infrastructure \\
Ego assignments & 2 (both mobile nodes, same recording) \\
Annotated objects & 2 static, single anchored map frame \\
Modalities & LiDAR, stereo depth, RGB-D, radar (2 mobile + 1 infra), \\
          & WiFi RSSI / CSI per frame, NTP offsets \\
Released geometry & per-frame occlusion, visibility, evidence counts, $C$, $\beta$, \\
          & Fresnel clearance and link state, occlusion episodes \\
\midrule
Deliberate difficulty & viewpoint-limited ego; \SI{1.53}{\metre} elevation offset \\
          & (IQR \SIrange{1.52}{1.54}{\metre}); AP positioned behind structure \\
Incidental difficulty & 9 / 8 occlusion episodes (median \SI{18.8}{\second}); \\
          & obstructed link on 33\,\% / 18\,\% of frames \\
\midrule
Not captured & scripted motion (Doppler paradigm undefined); \\
          & a second infrastructure node (handover under-sampled); \\
          & radar on \texttt{mobile\_2}; low-light and dropout conditions \\
\bottomrule
\end{tabular}
\end{table}

\todo{Extend once the staged runs land: scripted participants, low-light repeat,
node-outage repeat, and a bounded fully-annotated region. Report hours,
distance, frames, and annotated instances for the full release.}
