Synthetic check of the mobile_1 workflow (no bag, no ROS, no open3d needed):

    pip install numpy scipy matplotlib
    python3 tests/test_08_synthetic.py

The two `pipeline_*.py` files here are STUBS so the stage module imports;
never copy them next to the real pipeline.
